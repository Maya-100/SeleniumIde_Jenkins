pipeline {
    agent any

    stages{
        stageCheckout code {
            //checkout the repository
            steps {
                git branch: 'main', url: 'git://github.com/Maya-100/SeleniumIde_Jenkins'
            }
        }
        stage("Set up .Net Core") {
            //install .Net 
        }
        stage("Restore dependencies") {
           //install dependencies 
        }
        stage("Build") {
            //build
        }
        stage("Run test") {
            //run test
        }
    }
}
