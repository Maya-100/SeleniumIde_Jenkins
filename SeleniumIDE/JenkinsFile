pipeline {
    agent any

    stages{
        stageCheckout code {
            //checkout the repository
            steps {
                git branch: 'main', url: 'git://github.com/Maya-100/SeleniumIde_Jenkins'
            }
        
    }
}
